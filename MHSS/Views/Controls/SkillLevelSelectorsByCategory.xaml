<UserControl x:Class="MHSS.Views.Controls.SkillLevelSelectByCategory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MHSS.Views.Controls"
             xmlns:viewmodels="clr-namespace:MHSS.ViewModels.Controls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:SkillLevelSelectorsByCategoryViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Expander 
            HorizontalAlignment="Left" 
            Height="Auto" 
            IsExpanded="True"
            Margin="0,0,0,10" 
            VerticalAlignment="Top" 
            Width="auto"
            Background="Azure"
            BorderThickness="1"
            BorderBrush="Black"
        >
        <Expander.Header>
            <!-- TODO: Width10000はあまりよくないので別の方法があれば変えるべき -->
            <!--<TextBlock Text="{Binding Path=Head, Mode=OneWay}" Width="10000"/>-->
            <TextBlock Text="test" Width="10000"/>
        </Expander.Header>
        <TextBlock Text="test"></TextBlock>
    </Expander>
</UserControl>
