<UserControl x:Class="MHSS.Views.SubViews.Solution"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MHSS.Views.SubViews"
             xmlns:subview="clr-namespace:MHSS.ViewModels.SubView"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=subview:SolutionViewModel}"
             d:DesignHeight="200" d:DesignWidth="400">
    <Border BorderBrush="Gainsboro" BorderThickness="1">
        <Expander Grid.Column="0" Margin="0,0,0,0" Height="Auto" Width="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                  BorderThickness="0" BorderBrush="Transparent">
            <Expander.Header>
                <Grid HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}, Mode=OneWayToSource}"
                      Height="40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Viewbox Stretch="Uniform" Grid.Column="0" Height="20">
                        <TextBlock Text="{Binding Weapon.Value, Mode=OneWay}" FontSize="16" Margin="0,0,10,0"/>
                    </Viewbox>
                    <Viewbox Stretch="Uniform" Grid.Column="1" Height="20">
                        <TextBlock Text="{Binding Head.Value, Mode=OneWay}" FontSize="16" Margin="0,0,10,0"/>
                    </Viewbox>
                    <Viewbox Stretch="Uniform" Grid.Column="2" Height="20">
                        <TextBlock Text="{Binding Body.Value, Mode=OneWay}" FontSize="16" Margin="0,0,10,0"/>
                    </Viewbox>
                    <Viewbox Stretch="Uniform" Grid.Column="3" Height="20">
                        <TextBlock Text="{Binding Arm.Value, Mode=OneWay}" FontSize="16" Margin="0,0,10,0"/>
                    </Viewbox>
                    <Viewbox Stretch="Uniform" Grid.Column="4" Height="20">
                        <TextBlock Text="{Binding Waist.Value, Mode=OneWay}" FontSize="16" Margin="0,0,10,0"/>
                    </Viewbox>
                    <Viewbox Stretch="Uniform" Grid.Column="5" Height="20">
                        <TextBlock Text="{Binding Leg.Value, Mode=OneWay}" FontSize="16" Margin="0,0,10,0"/>
                    </Viewbox>
                    <Viewbox Stretch="Uniform" Grid.Column="6" Height="20">
                        <TextBlock Text="{Binding Charm.Value, Mode=OneWay}" FontSize="16" Margin="0,0,10,0"/>
                    </Viewbox>
                </Grid>
            </Expander.Header>
            <StackPanel>
                <TextBlock Text="{Binding Weapon.Value, Mode=OneWay}" Height="Auto" Width="200" Background="WhiteSmoke" FontSize="20" HorizontalAlignment="Left" Margin="20,0,0,0" Foreground="Black"/>
                <TextBlock Text="{Binding Head.Value, Mode=OneWay}" Height="Auto" Width="200" Background="White" FontSize="20" HorizontalAlignment="Left" Margin="20,0,0,0" Foreground="Black"/>
                <TextBlock Text="{Binding Body.Value, Mode=OneWay}" Height="Auto" Width="200" Background="WhiteSmoke" FontSize="20" HorizontalAlignment="Left" Margin="20,0,0,0" Foreground="Black"/>
                <TextBlock Text="{Binding Arm.Value, Mode=OneWay}" Height="Auto" Width="200" Background="White" FontSize="20" HorizontalAlignment="Left" Margin="20,0,0,0" Foreground="Black"/>
                <TextBlock Text="{Binding Waist.Value, Mode=OneWay}" Height="Auto" Width="200" Background="WhiteSmoke" FontSize="20" HorizontalAlignment="Left" Margin="20,0,0,0" Foreground="Black"/>
                <TextBlock Text="{Binding Leg.Value, Mode=OneWay}" Height="Auto" Width="200" Background="White" FontSize="20" HorizontalAlignment="Left" Margin="20,0,0,0" Foreground="Black"/>
                <TextBlock Text="{Binding Charm.Value, Mode=OneWay}" Height="Auto" Width="200" Background="WhiteSmoke" FontSize="20" HorizontalAlignment="Left" Margin="20,0,0,0" Foreground="Black"/>
                <TextBlock Text="{Binding disp.Value, Mode=OneWay}" Height="Auto" Width="200" Background="White" FontSize="12" Margin="10,0,0,0" Foreground="Black"/>
            </StackPanel>
        </Expander>
    </Border>
</UserControl>
